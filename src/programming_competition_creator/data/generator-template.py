# Note that the function MUST be named generate
def generate():
    # Replace with a random test case generator for the problem that outputs this same format
    return [
        {"input": "", "answer": "Hello, world!"},
    ]
